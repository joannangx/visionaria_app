%div{:class => "comment" }
    %div{:class => "user" }
        %p= comment.username
    %div{:class => "body"}
        %p= comment.body
    %div{:class => "timestamp" }
        %p= comment.created_at.strftime("%m/%d/%Y at %l:%M%p")
%br